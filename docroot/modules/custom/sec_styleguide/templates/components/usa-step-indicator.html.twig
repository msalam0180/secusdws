{% set component_details = module_path ~ '/templates/component_details.html.twig' %}
{% include component_details %}

{% set example_1 = pattern_preview('step_indicator') %}

{{ example_1|raw }}

{# {{ pattern('accordion', {
  unique_id: 'example_1',
  style: 'bordered',
  accordion_sections: [
    {
      heading: 'Code',
      body: "<pre><code>#{example_1|e}</code></pre>",
      expanded: 'false',
    }
  ]
}) }} #}
