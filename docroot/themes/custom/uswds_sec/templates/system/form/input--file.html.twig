{% extends "input.html.twig" %}

{# Overrides to adhere to USWDS code.#}
{% set attributes = attributes.addClass(classes, "usa-file-input").setAttribute('aria-describedby', element["#id"] ~ '-hint') %}